# s.yaml
edition: 1.0.0
name: resume-analyzer-project
access: "default"

services:
  resume-analyzer-service:
    component: fc
    props:
      region: cn-hangzhou
      service:
        name: sidereus-ai
      function:
        name: resume-api
        description: 'FastAPI Resume Analyzer'
        runtime: python3.10
        memorySize: 1024
        timeout: 600
        codeUri: ./
        environmentVariables:
          MY_REAL_API_KEY: "API_KEY"
          DEEPSEEK_BASE_URL: "https://api.deepseek.com"
        handler: main.handler 
      triggers:
        - name: httpTrigger
          type: http
          config:
            authType: anonymous
            methods: [GET, POST, OPTIONS]
